

<div class="container mx-auto px-4  rounded-md">

    {{ form_start(form, {'attr': {'class': 'mt-4'}}) }}
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div class="w-full">
                <h4>Email</h4>
                {{ form_widget(form.email, {'attr': {'class': 'p-2 border rounded w-full'}}) }}
            </div>
            <div class="w-full">
                <h4>Mot de passe</h4>
                {{ form_widget(form.Password, {'attr': {'class': 'p-2 border rounded w-full'}}) }}
            </div> 
            <div class="w-full">
                <h4>Nom</h4>
                {{ form_widget(form.name, {'attr': {'class': 'p-2 border rounded w-full'}}) }}
            </div>
           
            
        </div>
    </div>
        <button class="text-white text-center font-bold text-xl px-6   
        py-2 rounded-full hover:bg-blue-300 hover:text-black duration-300 bg-blue-700 inline-block mt-6">{{ button_label|default('Enregister')}}</button>
    {{ form_end(form) }}
    </div>
    